// Ambient stub for the deprecated @cyoda/cobi-react package.
// See ../../reporting-react/src/cobi-react.d.ts for the rationale; the same
// approach is used here because RangeCondition imports `FilterBuilderCondition`,
// `FilterCondition`, and `ColumnInfo` from cobi-react at runtime.
declare module '@cyoda/cobi-react' {
  export const FilterBuilderCondition: any
  export type FilterCondition = any
  export type ColumnInfo = any
}
